# Agent Instructions

The canonical guidance for this repository lives in [`CLAUDE.md`](./CLAUDE.md).

It covers project layout, architecture, build/test commands, the AI workflow
(`/prd` -> `/design` -> `/tasks` -> `/process`) and orchestrator pattern,
conventions, the branching and release process, and issue/PR/milestone hygiene.
Read it before making changes.
